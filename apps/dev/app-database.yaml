apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: so1s-database-app-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: so1s-project-dev

  source:
    repoURL: https://github.com/So1S/deploy.git
    targetRevision: SO1S-498-frontend-각-테이블-페이지-별-age-시간대-수정
    path: charts/database

    helm:
      releaseName: database
      valueFiles:
        - dev-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: backend

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
