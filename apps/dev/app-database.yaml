apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: so1s-database-app-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: so1s-project-dev

  source:
    repoURL: https://github.com/So1S/deploy.git
    targetRevision: SO1S-280-deploy-온프레미스-차트-개발
    path: charts/database

    helm:
      releaseName: database
      valueFiles:
        - dev-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: backend

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
