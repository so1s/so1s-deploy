apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: so1s-istio-app-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: so1s-project-dev

  source:
    repoURL: git@github.com:So1S/deploy.git
    targetRevision: SO1S-352-deploy-istio-서브차트-통합-및-root-dev-auto-sync-활성화
    path: charts/istio

    helm:
      releaseName: istio
      valueFiles:
        - dev-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: istio-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
