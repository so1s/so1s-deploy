apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: so1s-database-app-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: so1s-project-prod

  source:
    repoURL: https://github.com/So1S/deploy.git
    targetRevision: release/v0.4.1
    path: charts/database

    helm:
      releaseName: database
      valueFiles:
        - prod-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: backend

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
