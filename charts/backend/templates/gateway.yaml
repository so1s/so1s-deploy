apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: {{ include "backend.fullname" . }}
spec:
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    {{- include "ingress.hosts" . | nindent 4 }}