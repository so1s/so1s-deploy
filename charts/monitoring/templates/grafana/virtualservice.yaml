apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "grafana.fullname" . }}
spec:
  hosts:
    {{- include "grafana.ingress.hosts" . | nindent 4 }}
  gateways:
  - {{ include "grafana.fullname" . }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        port:
          number: 80
        host: {{ include "grafana.fullname" . }}