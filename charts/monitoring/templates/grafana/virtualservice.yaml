apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "kube-prometheus-stack.fullname" . }}-grafana
spec:
  hosts:
    {{ toYaml .Values.grafana.ingress.hosts | indent 4 }}
  gateways:
  - {{ include "kube-prometheus-stack.fullname" . }}-grafana
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        port:
          number: 80
        host: {{ include "kube-prometheus-stack.fullname" . }}-grafana